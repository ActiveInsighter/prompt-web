name: Inspect AI Search namespace

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies quietly
        run: npm install --no-package-lock --no-audit --no-fund >/dev/null

      - name: Inspect namespaces and instances
        shell: bash
        run: |
          set -euo pipefail
          test -n "$CLOUDFLARE_API_TOKEN" || { echo 'Missing CLOUDFLARE_API_TOKEN'; exit 1; }
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo 'Missing CLOUDFLARE_ACCOUNT_ID'; exit 1; }

          echo 'NAMESPACES='
          npx wrangler ai-search namespace list --json 2>/dev/null | jq -c '.'

          echo 'PROMPT_PROJECTS_NAMESPACE='
          npx wrangler ai-search namespace get prompt-projects --json 2>/dev/null | jq -c '.'

          echo 'PROMPT_PROJECTS_INSTANCES='
          npx wrangler ai-search list --namespace prompt-projects --json 2>/dev/null | jq -c '.'

          echo 'DEFAULT_INSTANCES='
          npx wrangler ai-search list --namespace default --json 2>/dev/null | jq -c '.'
